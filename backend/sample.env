# -----------------------------------------------------------------------------
# MongoDB Configuration
# -----------------------------------------------------------------------------
# This is the connection string for your MongoDB Atlas cluster.
# IMPORTANT: Replace <username>, <password>, and your cluster details.
# You can get this from the Atlas dashboard: "Connect" > "Drivers".
MONGO_DETAILS="mongodb+srv://<username>:<password>@yourcluster.mongodb.net/?retryWrites=true&w=majority"
DATABASE_NAME="novazure-dev"

# -----------------------------------------------------------------------------
# FastAPI Application Settings
# -----------------------------------------------------------------------------
# These settings control the information in the API documentation (/docs).
APP_TITLE="NovAzure API"
APP_VERSION="0.1.0"
APP_DESCRIPTION="Backend services for the NovAzure sustainability platform."

# -----------------------------------------------------------------------------
# CORS (Cross-Origin Resource Sharing) Settings
# -----------------------------------------------------------------------------
# A comma-separated list of origins allowed to make requests to this API.
# For local development, this should be your frontend's address.
# Example: "http://localhost:3000,http://127.0.0.1:3000"
CLIENT_ORIGIN_URL="http://localhost:3000"

# -----------------------------------------------------------------------------
# Security & JWT Token Settings (For future authentication)
# -----------------------------------------------------------------------------
# SECRET_KEY: A long, random string for signing JWTs.
# Generate one using: openssl rand -hex 32
SECRET_KEY="your_super_secret_random_string_goes_here"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=60 